# Third-party notices

SkyNet contains and depends on the following third-party projects. Their own
licenses and notices remain authoritative.

- **CommonLibSSE-NG** — included as the `extern/CommonLibSSE-NG` Git submodule.
  See its repository and license files after initializing submodules.
- **Microsoft WebView2 SDK** — SDK headers and static loader library are included
  under `extern/WebView2`. Players still install the WebView2 Runtime separately.
- **cpp-httplib** — included as `include/httplib.h`; see its license notice in
  that file.
- **uBlock Origin Lite** by Raymond Hill — the bundled SkyNet AdBlocker is a
  renamed distribution of this component. It remains under GPL-3.0; its complete
  notice is retained at `assets/Extensions/SkyNetAdBlocker/LICENSE.txt`.
